# Resolve remaining dependency vulnerabilities

1. Align every Expo package with SDK 57 and refresh published dependency fixes.
2. Apply the audited `image-size` denial-of-service patch and a targeted safe UUID override.
3. Verify a clean install, zero-vulnerability audit, malformed-image regressions, lint, types, and Expo export.
4. Publish the remediation pull request and verify its hosted checks.
